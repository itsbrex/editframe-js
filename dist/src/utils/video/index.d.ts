import { LayerFormat, LayerHorizontalAlignment } from 'constant';
export declare const validateLayerFormat: (format: LayerFormat) => void;
export declare const validateTextAlignment: (textAlignment: LayerHorizontalAlignment) => void;
